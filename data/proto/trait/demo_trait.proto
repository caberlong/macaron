syntax = "proto3";
  
package caber.macaron;

// Represents demographics traits associated with a financial entity.
message DemoTrait {
  // A global unique ID for this finance entity.
  int32 fin_entity_id = 1;

  int32 population = 2;
}
